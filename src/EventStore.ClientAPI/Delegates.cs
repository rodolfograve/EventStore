using System.Threading.Tasks;

namespace EventStore.ClientAPI
{
    //public static class Delegates
    //{
    //    // Func<EventStoreSubscription, ResolvedEvent, Task>
    //    public delegate Task EventAppearedOnEventStoreSubscription(EventStoreSubscription subscription, ResolvedEvent resolvedEvent);

    //    // Func<EventStoreCatchUpSubscription, ResolvedEvent, Task>
    //    public delegate Task EventAppearedOnEventStoreCatchUpSubscription(EventStoreCatchUpSubscription subscription, ResolvedEvent resolvedEvent);

    //    // Func<EventStorePersistentSubscription, ResolvedEvent, Task>
    //    public delegate Task EventAppearedOnEventStorePersistentSubscription(PersistentEventStoreSubscription subscription, ResolvedEvent resolvedEvent);

    //    // EventAppearedOnEventStorePersistentSubscriptionBase
    //    public delegate Task EventAppearedOnEventStorePersistentSubscriptionBase(EventStorePersistentSubscriptionBase subscription, ResolvedEvent resolvedEvent);
    //}
}
